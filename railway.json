{
    "$schema": "https://railway.app/railway.schema.json",
    "build": { "builder": "DOCKERFILE" },
    "deploy": {
      "startCommand": "cd WasslPoint && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn WasslPoint.wsgi:application --bind 0.0.0.0:$PORT"
    },
    "variables": {
      "DATABASE_URL": "${{Postgres.DATABASE_URL}}"
    }
  }